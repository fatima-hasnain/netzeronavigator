# Optional: CDN or alternate host for /models/... static assets (no trailing slash).
# Leave unset to load from the same origin (Vite dev server serves `public/`).
# VITE_MODELS_BASE=https://example.com/static
